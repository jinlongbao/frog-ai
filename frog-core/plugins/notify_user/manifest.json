{
  "id": "notify_user",
  "name": "Notify User",
  "description": "Send a desktop system notification to the user. Use this when you complete a long-running task, encounter an important finding, or need to alert the user to something without interrupting the task flow. The notification appears as a Windows/macOS toast popup.",
  "version": "1.0.0",
  "entry": "index.py",
  "source": "builtin",
  "parameters": {
    "type": "object",
    "properties": {
      "title": {
        "type": "string",
        "description": "Notification title (short, max 50 chars)"
      },
      "message": {
        "type": "string",
        "description": "Notification body text (max 200 chars)"
      },
      "urgency": {
        "type": "string",
        "description": "Urgency level: 'normal' or 'critical'. Default: 'normal'",
        "default": "normal"
      }
    },
    "required": ["title", "message"]
  }
}
