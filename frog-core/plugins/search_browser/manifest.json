{
  "id": "search_browser",
  "name": "Intelligent Web Researcher",
  "version": "1.0.0",
  "description": "Searches the web, learns from multiple pages, and provides a summarized report.",
  "author": "Frog Core",
  "entry_point": "index.py",
  "parameters": {
    "type": "object",
    "properties": {
      "command": {
        "type": "string",
        "description": "The search query or research topic."
      },
      "max_results": {
        "type": "integer",
        "description": "Maximum search results to retrieve (default 5).",
        "default": 5
      },
      "max_learn": {
        "type": "integer",
        "description": "Maximum pages to learn in depth (default 3).",
        "default": 3
      }
    },
    "required": ["command"]
  }
}
